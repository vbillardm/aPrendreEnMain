{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editer le nom du compte ulule</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}
    <a href="{{ path('acceuil_admin_index') }}"><button class="btn btn-primary">Retour au panneau d'administration</button></a>
{% endblock %}
