{% extends 'base.html.twig' %}

{% block body %}
  {% include 'navbaradmin.html.twig' %}
    <h1>News creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <a href="{{ path('news_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
{% endblock %}
