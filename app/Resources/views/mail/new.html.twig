{% extends 'base.html.twig' %}

{% block body %}
  {% include 'navbaradmin.html.twig' %}
    <h1>Envoyer une newsletter</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Envoyer" />
    {{ form_end(form) }}

    <a href="{{ path('newsletter_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
{% endblock %}
